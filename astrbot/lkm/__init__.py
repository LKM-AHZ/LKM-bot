"""LKM 定制层：本 fork 相对上游 AstrBot 新增的、与社区站配套的接入代码。

集中放在这里是为了把「本地定制」与「上游命名空间」分开：上游文件尽量保持原样，便于后续
merge 上游；定制代码集中一处，改部署形态时只需看这一个包。

依赖方向**只能**是 ``astrbot.*`` → ``astrbot.lkm``。本包内不得在模块级 import
``astrbot.dashboard``（面板侧依赖本包，反向 import 会构成循环；sso 需要在请求路径上取面板
实现时用函数内惰性 import）。
"""
