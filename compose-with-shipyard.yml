# 当接入 QQ NapCat 时，请使用这个 compose 文件一键部署: https://github.com/NapNeko/NapCat-Docker/blob/main/compose/astrbot.yml

services:
  lkmbot:
    build: .
    image: lkmbot:latest
    container_name: lkmbot
    restart: always
    ports: # mappings description: https://github.com/AstrBotDevs/AstrBot/issues/497
      - "6185:6185" # 必选，LKMBot WebUI 端口
      - "6199:6199" # 可选, QQ 个人号 WebSocket 端口
    environment:
      - TZ=Asia/Shanghai
    volumes:
      - ${PWD}/data:/LKMBot/data
      # - /etc/timezone:/etc/timezone:ro
      - /etc/localtime:/etc/localtime:ro
    networks:
      - lkmbot_network

  shipyard:
    image: soulter/shipyard-bay:latest
    container_name: lkmbot_shipyard
    # ports:
    #   - "8156:8156"
    environment:
      - PORT=8156
      - DATABASE_URL=sqlite+aiosqlite:///./data/bay.db
      - ACCESS_TOKEN=secret-token
      - MAX_SHIP_NUM=10
      - BEHAVIOR_AFTER_MAX_SHIP=reject
      - DOCKER_IMAGE=soulter/shipyard-ship:latest
      - DOCKER_NETWORK=lkmbot_network
      - SHIP_DATA_DIR=${PWD}/data/shipyard/ship_mnt_data
      - DEFAULT_SHIP_CPUS=1.0
      - DEFAULT_SHIP_MEMORY=512m
    volumes:
      - ${PWD}/data/shipyard/bay_data:/app/data
      - ${PWD}/data/temp:/LKMBot/data/temp           # Bind the local temp directory to the sandbox so that the uploaded file can be accessed in the sandbox
      - /var/run/docker.sock:/var/run/docker.sock:ro
    networks:
      - lkmbot_network

networks:
  lkmbot_network:
    name: lkmbot_network
    driver: bridge
